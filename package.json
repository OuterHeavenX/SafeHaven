{
  "name": "safehaven",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "compile": "tsc",
    "serve": "node tools/static-server/server.mjs",
    "test": "node --test tests/**/*.test.mjs",
    "clean": "node tools/build/clean.mjs",
    "build": "npm run clean && npm run typecheck && npm run compile && node tools/build/prepare-static.mjs"
  },
  "dependencies": {
    "playcanvas": "^2.21.1"
  },
  "devDependencies": {
    "typescript": "^5.8.3"
  }
}
