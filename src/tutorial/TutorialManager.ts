import type{WorldState}from'../types/GameTypes';export class TutorialManager{private shown=-1;update(s:WorldState,notify:(s:string)=>void){let step=s.tutorialStep;if(step===0){notify('Welcome to the Fallen Valley. Pan the camera, then select the Construction Wagon.');step=1}else if(step===1&&!s.units.some(u=>u.kind==='wagon'&&u.alive)){notify('The Haven Keep is deploying. Build an Aether Generator to establish Power.');step=2}else if(step===2&&s.buildings.some(b=>b.kind==='aetherGenerator'&&b.construction>=1)){notify('Power restored. Build a Gold Refinery and protect your economy.');step=3}else if(step===3&&s.buildings.some(b=>b.kind==='goldRefinery'&&b.construction>=1)){notify('Train a Gatherer, then select a glowing Gold deposit to begin hauling.');step=4}else if(step===4&&s.buildings.some(b=>b.kind==='barracks'&&b.construction>=1)){notify('Train Wardens and Crossbowmen. The Covenant is watching.');step=5}else if(step===5&&s.units.filter(u=>u.faction==='haven'&&['warden','crossbowman'].includes(u.kind)).length>=7){notify('Military foothold established. Explore east, secure Essence, and destroy the Covenant Fortress.');step=20}if(step!==s.tutorialStep)s.tutorialStep=step}}