import{GameApp}from'./app/GameApp';async function main(){const canvas=document.querySelector<HTMLCanvasElement>('#game-canvas');if(!canvas)throw new Error('Missing #game-canvas');const game=new GameApp(canvas);await game.init()}main().catch(e=>{console.error(e);const el=document.querySelector<HTMLElement>('#fatal-error');if(el){el.hidden=false;el.innerHTML=`<h2>SafeHaven could not start</h2><pre>${String(e instanceof Error?e.stack??e.message:e)}</pre><p>Check WebGL2/WebGPU support and the developer console.</p>`}});